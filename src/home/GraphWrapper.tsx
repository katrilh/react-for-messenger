import React from "react";
import { Text, Card } from "rebass/styled-components";

const GraphWrapper = () => (
  <Card padding={3} marginTop={2}>
    <Text color="primary"> Maybe one day there will be a graph here</Text>
  </Card>
);

export default GraphWrapper;
