Put all facebook msg folders in this folder

You can run this python script to get a list of all folders 
in  `chats.ts` which are imported into `fileloader.ts`


import os

# For now, this is where you can list all copy all
# facebook folders into fileloader.ts

# Run this script, and it will create a chats.ts file
# It will export the consts with all the chats, just save
# and run the app


def main():
    names = os.listdir('src/datafiles')
    with open("src/datafiles/chats.ts", "w") as f:
        f.write('export const chats: string[] = ['
                )
        for n in names:
            if n.find('.') == -1:
                f.write(f"'{n}', \n")
        f.write("] ")


if __name__ == '__main__':
    main()
